<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <%= link_to "Data Migration Tool", root_path, class: "navbar-brand" %>

    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <%= link_to "Migration Plans", migration_plans_path, class: "nav-link" %>
        </li>
        <li class="nav-item">
          <%= link_to "Execution History", migration_executions_path, class: "nav-link" %>
        </li>
        <% if current_user.can_manage_users? %>
          <li class="nav-item">
            <%= link_to "Users", users_path, class: "nav-link" %>
          </li>
        <% end %>
      </ul>

      <ul class="navbar-nav">
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-bs-toggle="dropdown">
            <%= current_user.email %>
            <span class="badge bg-<%= current_user.admin? ? 'danger' : (current_user.operator? ? 'warning' : 'secondary') %>">
              <%= current_user.role.titleize %>
            </span>
          </a>
          <ul class="dropdown-menu dropdown-menu-end">
            <li><%= link_to "Sign Out", destroy_user_session_path, data: { turbo_method: :delete }, class: "dropdown-item" %></li>
          </ul>
        </li>
      </ul>
    </div>
  </div>
</nav>
